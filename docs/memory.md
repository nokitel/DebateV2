# Memory Model

Memory is markdown-first: `memory/decisions/`, `memory/mistakes.md`, `memory/glossary.md`, `memory/guardrails.md`, plus generated `memory/index.json`. Entries use provenance frontmatter, supersede instead of overwrite, and treat contradictions as first-class.
