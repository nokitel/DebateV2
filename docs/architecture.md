# Architecture

AI Harness is a standalone skeleton repo. `skeleton/` is copied into target projects; `bootstrap/` installs/upgrades it; `tests/` validates the skeleton itself.

Core flow: brief → research → synthesis → clarification → scope lock → spec/plan/slices → implementation → acceptance verification → quality review → integration → final validation → lessons.
