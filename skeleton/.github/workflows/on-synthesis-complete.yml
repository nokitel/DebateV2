# Trigger: see docs/state-machine.md
# Inputs: GitHub event payload for on-synthesis-complete
# Outputs: comments, status changes, metrics lines
# Failure modes: post harness:runner-error comment and metrics JSONL line
name: on-synthesis-complete
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO on-synthesis-complete"
