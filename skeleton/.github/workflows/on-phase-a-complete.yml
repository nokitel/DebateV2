# Trigger: see docs/state-machine.md
# Inputs: GitHub event payload for on-phase-a-complete
# Outputs: comments, status changes, metrics lines
# Failure modes: post harness:runner-error comment and metrics JSONL line
name: on-phase-a-complete
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO on-phase-a-complete"
