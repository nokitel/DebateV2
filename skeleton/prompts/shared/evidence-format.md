# Evidence Format

Done reports include files changed, commands run, results, skipped checks with reasons, remaining risks, and evidence links.

Example 1: `npm test` passed, screenshot attached.
Example 2: `npm run e2e` skipped because browser dependency unavailable; risk noted.
