Alembic migration directory. The current development build also creates tables with SQLAlchemy metadata on startup for convenience, but production schema changes should be applied through these revisions.

