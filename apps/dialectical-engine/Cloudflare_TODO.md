# Cloudflare TODO

The active checklist is `Cloudfare_TODO.md`.

This correctly spelled filename exists only as a pointer so the Cloudflare setup
notes are easy to find. The intentionally misspelled `Cloudfare_TODO.md` is
kept because that was the requested filename.
